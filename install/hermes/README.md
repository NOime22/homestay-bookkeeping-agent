# Hermes Installation Notes

Suggested Hermes-side usage:
- treat this package as an explicit project asset
- load the core markdown files into the active bookkeeping workflow
- keep Hermes-specific debugging notes out of the core package when possible
- use Hermes only as the current host and testing environment
